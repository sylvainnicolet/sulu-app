{% extends "base.html.twig" %}

{% block content %}
    <div class="container mx-auto p-12 text-center">
	    <h1 class="text-4xl font-bold mb-4">{{ content.main_title }}</h1>
	    {{ content.article|raw }}
    </div>
{% endblock %}
